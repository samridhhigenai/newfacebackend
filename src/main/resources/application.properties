# Server Configuration
server.port=${PORT:8082}
server.address=0.0.0.0
server.servlet.context-path=/api

# MongoDB Configuration - Updated Database
spring.data.mongodb.uri=mongodb+srv://samridhhigenai:HxMq2SWWdG2CIJ7w@cluster0.2fxx1iv.mongodb.net/?retryWrites=true&w=majority
spring.data.mongodb.database=test

# JPA Configuration (keeping for compatibility)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# MongoDB SSL Configuration
spring.data.mongodb.ssl.enabled=true
spring.data.mongodb.ssl.invalid-hostname-allowed=true

# Connection Pool Settings
spring.data.mongodb.option.connections-per-host=10
spring.data.mongodb.option.min-connections-per-host=1
spring.data.mongodb.option.max-connection-idle-time=60000
spring.data.mongodb.option.max-connection-life-time=120000
spring.data.mongodb.option.connect-timeout=30000
spring.data.mongodb.option.socket-timeout=30000
spring.data.mongodb.option.server-selection-timeout=30000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.faceattendance=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.mongodb.driver=DEBUG

# CORS Configuration
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Face Recognition Configuration - WORKING THRESHOLD LIKE DEMO_FACE_06_JULY
face.recognition.confidence.threshold=50.0
face.recognition.model.path=models/
face.recognition.cascade.path=haarcascade_frontalface_alt.xml
